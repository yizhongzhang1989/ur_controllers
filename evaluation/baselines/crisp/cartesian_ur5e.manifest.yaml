# Manifest for a crisp_controllers regulation-hold baseline rosbag.
# Generated by scripts/record_crisp_baseline.sh; bag payload is
# gitignored, only this manifest is committed.
schema_version: 1
scenario: regulation_hold
role: "cartesian"
controller_name: "cartesian_impedance_controller"
robot: "ur5e"
duration_s: 10
recorded_at: "2026-04-23T01:19:03Z"
bag:
  path: "evaluation/baselines/crisp/cartesian_ur5e.bag"
  storage_id: sqlite3
  committed: false  # payload is gitignored
sim:
  backend: mujoco
  control_mode: effort
  ur_type: "ur5e"
  # ur_simulator does not expose a separate RNG seed; the initial
  # joint configuration is determined by its config.yaml.
  seed: null
bringup:
  launch: bringup/launch/crisp_bringup.launch.py
  args:
    robot: "ur5e"
    mode: "cartesian"
  config: "bringup/config/crisp_cartesian_impedance.ur5e.yaml"
topics:
  - "/joint_states"
  - "/cartesian_impedance_controller/tau_d"
bag_contents:
  - name: "/joint_states"
    type: "sensor_msgs/msg/JointState"
    message_count: 4513
recorder:
  script: scripts/record_crisp_baseline.sh
  version: 1
notes: >
  Regulation hold: on_activate the CartesianController captures the
  current tool0 pose as its Cartesian target and the current joint
  config as the nullspace target. No external target is published.
